## What I've learned

Find out about the null operator "??" If API would return undefined then I would make array of movies epmpty
in order not to break application
Why also used data.Response intead of if(!res.ok) ? Because this API returns always 200 status code even if a movie does not exist

Also find out about text-base and container classes in tailwind (yes I didn't know about them)
